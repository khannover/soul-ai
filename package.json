{
  "name": "soul-ai",
  "version": "2.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:css": "tailwindcss -i ./src/tailwind-input.css -o ./tailwind.css --minify",
    "tls": "bash scripts/setup-tls.sh",
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 -m http.server 8080"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  },
  "dependencies": {
    "tls": "^0.0.1"
  }
}
